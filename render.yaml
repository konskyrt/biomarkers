services:
  - type: web
    name: bim-data-dashboard       # must match the service Render already has
    rootDir: materialauszug        # <─ NEW build directory
    env: node
    buildCommand: |
      yarn install
      yarn build                   # or npm ci && npm run build
    startCommand: |
      yarn start                   # or serve -s build, etc.